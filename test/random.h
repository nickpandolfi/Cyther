/** Just some function prototypes of the random.c source code
 * @author Nicholas C. Pandolfi
 */

// Function prototypes

char random_character();
char *random_string(size_t length);
int random_number(int start, int end);
